interface:
  display_name: "Deploy TestFlight"
  short_description: "Ship Luego iOS builds to TestFlight"
  default_prompt: "Use $deploy-testflight to bump Luego to todays date, increment the build number, archive with automatic signing, and upload to TestFlight."
